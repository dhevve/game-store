export interface IArr {
    id: number,
    name: string,
    price: number,
    img?: string
    year: number
}

export interface IUsers {
    id?: number,
    name: any,
    password: any,
    admin?: boolean
}

