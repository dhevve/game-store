export interface IArr {
    id: number,
    name: string,
    price: number | string,
    img?: string
}

export interface IUsers {
    id?: number,
    name: any,
    password: any,
    admin?: boolean
}

